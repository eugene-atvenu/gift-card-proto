meta {
  name: List Giftcards
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/companies/{{companyId}}/giftcards
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
