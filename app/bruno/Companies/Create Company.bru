meta {
  name: Create Company
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/companies
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "name": "Test Company",
    "metadata": {
      "industry": "retail"
    }
  }
}
